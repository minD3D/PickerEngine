package org.engine.pickerengine.dto;

public record InstagramSearchHashtag(
        String name,
        String id,
        Integer mediaCount,
        String profilePicUrl,
        String subtitle
) {
}
